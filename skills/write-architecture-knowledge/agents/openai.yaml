interface:
  display_name: "Write Arch Knowledge"
  short_description: "Create reusable architecture knowledge"
  default_prompt: "Use $Write Arch Knowledge to create reusable Architecture Knowledge not tied to one company."
policy:
  allow_implicit_invocation: false
