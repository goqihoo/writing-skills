interface:
  display_name: "Write Product Roadmap"
  short_description: "Sequence outcomes, bets, and dependencies"
  default_prompt: "Use $write-product-roadmap, $reason-product, and $write-doc to sequence product outcomes, bets, dependencies, and review points."
policy:
  allow_implicit_invocation: false
