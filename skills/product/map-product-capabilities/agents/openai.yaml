interface:
  display_name: "Map Product Capabilities"
  short_description: "Model stable product capabilities and ownership"
  default_prompt: "Use $map-product-capabilities, $reason-product, and $write-doc to model this product's stable capabilities and responsibility boundaries."
policy:
  allow_implicit_invocation: false
