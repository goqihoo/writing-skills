interface:
  display_name: "Write Product Strategy"
  short_description: "Define product direction, choices, and boundaries"
  default_prompt: "Use $Write Product Strategy and $Reason Product to define this product's strategy and explicit choices."
policy:
  allow_implicit_invocation: false
