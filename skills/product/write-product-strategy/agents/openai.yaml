interface:
  display_name: "Write Product Strategy"
  short_description: "Define product direction, choices, and boundaries"
  default_prompt: "Use $write-product-strategy, $reason-product, and $write-doc to define this product's strategy and explicit choices."
policy:
  allow_implicit_invocation: false
