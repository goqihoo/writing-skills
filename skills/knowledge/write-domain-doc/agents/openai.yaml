interface:
  display_name: "Write Domain Doc"
  short_description: "Create common documents in a domain directory"
  default_prompt: "Use $Write Domain Doc and $Reason Domain to create the appropriate document for this accepted domain knowledge set."
policy:
  allow_implicit_invocation: false
