interface:
  display_name: "Write Doc"
  short_description: "Apply shared structure and clarity rules"
  default_prompt: "Use $write-doc to structure this document and make its prose clear, natural, and easy to follow without changing its logic."
policy:
  allow_implicit_invocation: false
